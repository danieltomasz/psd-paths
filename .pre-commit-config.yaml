repos:
-   repo: https://github.com/kynan/nbstripout
    rev: 0.8.1
    hooks:
    -   id: nbstripout
        # Only clean files inside the 'WIP' folder
        files: ^(templates/|notebooks/)
        args: ["--extra-keys", "metadata.kernelspec"]
